---
description: 이번에 수행할 작업을 명시한 파일로, new chat 이 새로 시작하면 해당 내용 참고
globs:
alwaysApply: false
---

## 모노레포에서 JIT 방식도 설정하기

- 현재 Compiled 방식을 통해 carousel package 를 빌드해서 빌드된 내용을 movies 에서 참고해서 사용.
- dev 환경은 JIT 방식과 prod 환경은 compiled 방식을 사용하면 좋음
  -> 왜냐하면 개발시 디버깅할때는 JIT 가 좋고 (프로덕트 코드는 코드 mangle 및 축약등이 되어 디버깅이 힘듦) compiled 방식은 product 환경에서 하면 성능이 좋음

=> 따라서 JIT 방식과 Compiled 방식 모두 지원하도록 변경하기
